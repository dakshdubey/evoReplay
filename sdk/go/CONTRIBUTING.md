# Contributing to EvoReplay Go SDK

## 🛠️ Build & Test

### Prerequisites
- Go >= 1.20

### Build
```bash
go build ./...
```

### Test
```bash
go test -v ./...
```

## 📦 Publishing

Go modules are published via Git tags.

1.  **Tag Version**:
    ```bash
    git tag v0.1.0
    ```

2.  **Push**:
    ```bash
    git push origin v0.1.0
    ```
